{
  /*state = {
    defaultCounterValue: this.props.defaultCounterValue,
    incrementBy: this.props.incrementBy,
    counter: this.props.defaultCounterValue
  };

  handleClick = () => {
    this.setState({
      counter: this.state.counter + (this.props.incrementBy | 1)
    });
  };
*/
}
